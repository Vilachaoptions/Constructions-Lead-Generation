"""webcourse — discovery + extraction for non-Skool course platforms.

Starts as a generic discovery tool (open a browser, log in manually, dump a
page's structure) so a platform-specific parser (e.g. Kajabi) can be built from
the real page shape. Reuses the same transcript pipeline as the Skool extractor.
"""

__version__ = "0.1.0"
