# Copy to .env and fill in. .env is gitignored and never committed.

# Skool login credentials (required for private classrooms)
SKOOL_EMAIL=you@example.com
SKOOL_PASSWORD=your-password

# Only needed if you use --transcription-backend openai
OPENAI_API_KEY=
